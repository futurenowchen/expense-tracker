<div class="new-tittle d-flex justify-content-center my-5">
  <small class="col-1">*必填欄位</small>
  <h4 class="col-2 d-flex justify-content-center">請修改你的支出</h4>
</div>


<div class="container d-flex justify-content-center">
  <form action="/records/{{record._id}}?_method=PUT" method="POST">

    <div class="form-group d-flex mb-5">
      <label for="name" class="col-md-3 col-form-label">
        名稱*：
      </label>
      <div class="col-md-10">
        <input type="text" name="name" class="form-control" id="name" value="{{record.name}}" required>
      </div>
    </div>

    <div class="form-group d-flex mb-5">
      <label for="date" class="col-md-3 col-form-label">
        日期*：
      </label>
      <div class="col-md-10">
        <input type="date" name="date" class="form-control" id="date" value="{{record.date}}" required>
      </div>
    </div>

    <div class="form-group d-flex mb-5">
      <label for="category" class="col-md-3 col-form-label">
        類別*：
      </label>
      <div class="col-md-10">
        <select id="categories" name="categories" value="{{record.category}}">
          <option value="家居物業">家居物業</option>
          <option value="交通出行">交通出行</option>
          <option value="休閒娛樂">休閒娛樂</option>
          <option value="餐飲食品">餐飲食品</option>
          <option value="其他">其他</option>
        </select>
      </div>
    </div>

    <div class="form-group d-flex mb-5">
      <label for="amount" class="col-md-3 col-form-label">
        金額*：
      </label>
      <div class="col-md-10">
        <input type="number" name="amount" class="form-control" id="amount" value="{{record.amount}}" required>
      </div>
    </div>

    <div class="modal-footer">
      <button type="submit" class="btn btn-primary">修改支出</button>
    </div>
  </form>
</div>